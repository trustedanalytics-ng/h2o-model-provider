server.port: 56951

security:
  ignored: /**

sync:
  delay_seconds: 1

services:
  catalog: http://localhost:${server.port}
  catalog_user: user
  catalog_pass: pass

logging:
  level:
    org.springframework.security: DEBUG
    org.trustedanalytics: DEBUG

maximum_cache_size: 1000
cache_expiration_time_s: 300
