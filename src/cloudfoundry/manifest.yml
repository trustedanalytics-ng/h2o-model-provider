---
applications:
- name: h2o-model-provider
  memory: 512M
  disk_quota: 256M
  instances: 1
  host: h2o-model-provider
  path: target/h2o-model-provider-${version}.jar
  services: 
  - sso
  - service-exposer
  env:
    SPRING_PROFILES_ACTIVE: "cloud"
    VERSION: ${version}
